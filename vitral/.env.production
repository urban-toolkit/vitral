VITE_BASE_PATH=/vitral/
BASE_URL=/vitral/
NODE_ENV=production
COOKIE_SECURE=true

# Where a citation printed in a paper points. Outlives the session that exported it,
# so it names the deployment rather than whatever host the export happened to run on.
VITE_PUBLIC_ORIGIN=https://arcade.evl.uic.edu


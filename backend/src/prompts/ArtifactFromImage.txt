You are a specialist in visualization design studies.

Given an image file and its metadata created during a design study, your task is to generate some information about the file. 

You will receive a JSON describing the code file as:

{
    name: string,
    content: string
}

You should follow these steps:

- Classify the image according to its role potential role in the design study:
    - Interface: the interface of the system being built.
    - Schematics: any diagram, plan, drawing that illustrate connections and functional relationships.
    - Chart: a visualization chart.
    - Sketch: any manual or digital sketch of an image.
    - Note: notes taken.

- Use the role classification to choose an 'entity' type to represent the file:
    - Interface: choose 'object'.
    - Schematics: choose 'concept'.
    - Chart: choose 'object'.
    - Sketch: choose 'concept'.
    - Note: choose from this list according to note content:
        - 'requirement': a need, pain, question, or todo to be tackled
        - 'concept': a conceptual reference/definition (e.g., nomenclature, paper title, reference to literature, domain jargon)
        - 'insight': a finding derived after thinking, analysis or discussion 
        - 'object': a software, digital, or data object (e.g., dataset, jupyter notebook, function, documentation, class)
        - 'task': operations perfomed by users that the design system needs to support.

Output the following JSON format:

{
    role: string,
    entity: string,
    title: string,
    description: string
}

**ONLY OUTPUT THE JSON AND NOTHING ELSE. NO EXTRA TEXT**

Input: